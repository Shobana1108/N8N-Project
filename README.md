# n8n Render deployment helper

This repo contains a minimal wrapper to run n8n on Render (Node environment).
After Render has built and started n8n, import your workflow JSON inside the n8n UI:
Workflows → Import → Upload JSON
